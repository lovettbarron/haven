---
phase: 12-planning-artifact-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/schemas/week.schema.json
  - data/schedules/w30.json
  - data/schedules/w31.json
  - data/schedules/w32.json
  - data/schedules/w33.json
  - data/schedules/w40.json
autonomous: true
requirements: [PMNT-04, PMNT-05]

must_haves:
  truths:
    - "Vacation week schedules (W30-W33) flag that the family may be on vacation and neighbor handles child tasks"
    - "W40 schedule garlic task duration aligns with garlic-autumn-plan.md (20-30 min)"
    - "All weekly schedule JSON files still validate against the updated schema"
  artifacts:
    - path: "data/schemas/week.schema.json"
      provides: "Updated schema with optional vacation_note field"
      contains: "vacation_note"
    - path: "data/schedules/w30.json"
      provides: "Vacation-flagged week"
      contains: "vacation_note"
    - path: "data/schedules/w40.json"
      provides: "Garlic hero task with correct details"
      contains: "garlic"
  key_links:
    - from: "data/schemas/week.schema.json"
      to: "data/schedules/w30-w33.json"
      via: "schema defines vacation_note, data files use it"
      pattern: "vacation_note"
    - from: "data/schedules/w40.json"
      to: "docs/garlic-autumn-plan.md"
      via: "task details must align"
      pattern: "garlic"
---

<objective>
Add vacation flags to W30-W33 schedule files and align W40 garlic task with garlic-autumn-plan.md.

Purpose: Schedule data files should correctly flag vacation overlap and reflect accurate task details from source documentation.
Output: Updated week schema with optional vacation_note, four vacation weeks flagged, W40 garlic task aligned.
</objective>

<execution_context>
@/Users/albair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-planning-artifact-maintenance/12-RESEARCH.md
@data/schemas/week.schema.json
@docs/garlic-autumn-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vacation_note to schema and flag W30-W33</name>
  <files>data/schemas/week.schema.json, data/schedules/w30.json, data/schedules/w31.json, data/schedules/w32.json, data/schedules/w33.json</files>
  <action>
**Step 1: Update week.schema.json**
1. Read data/schemas/week.schema.json
2. Add an optional "vacation_note" string property to the top-level properties object with description: "Note about family vacation overlap -- neighbor handles child tasks per neighbor-vacation-guide.md"
3. Do NOT add it to the "required" array -- it must remain optional
4. Do NOT change any other schema fields

**Step 2: Add vacation_note to W30-W33**
1. Read each of w30.json, w31.json, w32.json, w33.json
2. Add a top-level "vacation_note" field to each with value: "Family may be on vacation W30-W33. Child-assigned tasks should be handled by neighbor per neighbor-vacation-guide.md."
3. Preserve all existing content in each file exactly as-is
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && node -e "const s=require('./data/schemas/week.schema.json'); if(!s.properties.vacation_note) throw new Error('missing'); console.log('schema OK');" && node -e "for(const w of [30,31,32,33]){const d=require('./data/schedules/w'+w+'.json'); if(!d.vacation_note) throw new Error('w'+w); console.log('w'+w+' OK');}"</automated>
  </verify>
  <done>week.schema.json has optional vacation_note property. All four vacation week files (w30-w33) contain the vacation_note field referencing the neighbor guide.</done>
</task>

<task type="auto">
  <name>Task 2: Align W40 garlic task with garlic-autumn-plan.md</name>
  <files>data/schedules/w40.json</files>
  <action>
1. Read data/schedules/w40.json and docs/garlic-autumn-plan.md
2. Compare the garlic task (W40-02) details against the garlic plan document
3. Apply these specific fixes:
   - Update W40-02 "minutes" from 15 to 25 (garlic-autumn-plan.md says 20-30 min, use midpoint)
   - Make garlic the primary hero_task text: "Plant garlic cloves for next year's harvest" (it is the significant seasonal event at W40; raspberry picking is routine by this point)
   - If no mulching follow-up task exists, add a brief note about straw mulch in the garlic task action text per the garlic plan
4. Preserve all other tasks and fields in w40.json exactly as-is
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && node -e "const d=require('./data/schedules/w40.json'); const gt=d.tasks.find(t=>t.id==='W40-02'); if(!gt) throw new Error('no garlic task'); if(gt.minutes < 20) throw new Error('minutes too low: '+gt.minutes); console.log('garlic task OK, minutes:', gt.minutes); console.log('hero:', d.hero_task);"</automated>
  </verify>
  <done>W40 garlic task duration is 25 minutes (aligned with garlic-autumn-plan.md range). Hero task text prioritizes garlic planting as the significant seasonal event.</done>
</task>

</tasks>

<verification>
- Schema validation: node require of week.schema.json succeeds and contains vacation_note property
- W30-W33 all contain vacation_note field
- W40 garlic task has 20-30 min duration and garlic-focused hero task
- No other schedule files are broken (they should pass schema since vacation_note is optional)
</verification>

<success_criteria>
Vacation weeks W30-W33 clearly flag neighbor override for child tasks. W40 garlic task details match the garlic-autumn-plan.md specifications. Schema updated to support the new field without breaking any existing schedule files.
</success_criteria>

<output>
After completion, create `.planning/phases/12-planning-artifact-maintenance/12-02-SUMMARY.md`
</output>
