---
phase: 12-planning-artifact-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PMNT-01, PMNT-02, PMNT-03]

must_haves:
  truths:
    - "Every completed plan checkbox in ROADMAP.md is checked"
    - "ROADMAP.md progress table has consistent column formatting across all rows"
    - "PROJECT.md Key Decisions shows no stale Revisit markers for completed work"
    - "REQUIREMENTS.md traceability shows no Pending status for delivered artifacts"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected checkboxes and progress table"
      contains: "- [x] 06-01-PLAN.md"
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions outcomes"
      contains: "Resolved"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected traceability statuses"
      contains: "DOCS-04.*Complete"
  key_links:
    - from: ".planning/ROADMAP.md progress table"
      to: "phase checkbox lines"
      via: "completion status must agree"
      pattern: "\\[x\\].*Complete"
---

<objective>
Fix all stale checkboxes, statuses, and metadata in the three core planning markdown files (ROADMAP.md, PROJECT.md, REQUIREMENTS.md).

Purpose: Planning artifacts should reflect actual project state so any future reader (human or AI) gets accurate context.
Output: Three updated markdown files with no stale markers for completed work.
</objective>

<execution_context>
@/Users/albair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-planning-artifact-maintenance/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md checkboxes and progress table</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read ROADMAP.md and apply these specific corrections:

**v1.0 section (inside the collapsed details block):**
- Change Phase 2 header from `[ ]` to `[x]` (completed 2026-03-27)
- Change Phase 3 header from `[ ]` to `[x]` (completed 2026-03-27)
- Change Phase 6 header from `[ ]` to `[x]` (completed 2026-03-28)
- Change 06-01-PLAN.md from `[ ]` to `[x]`
- Change 06-02-PLAN.md from `[ ]` to `[x]`

**v1.1 section:**
- Change 08-01-PLAN.md from `[ ]` to `[x]`
- Change 08-02-PLAN.md from `[ ]` to `[x]`
- Change 09-01-PLAN.md from `[ ]` to `[x]`
- Change 10-01-PLAN.md from `[ ]` to `[x]`
- Change 11-01-PLAN.md from `[ ]` to `[x]`
- Change 11-02-PLAN.md from `[ ]` to `[x]`

**Progress table fixes:**
- Phase 9 row: change "0/1" to "1/1", change "Not started" to "Complete", add completion date "2026-03-28"
- Phase 7, 8, 10, 11 rows: fix column alignment to match v1.0 row format (ensure Milestone column is present with "v1.1")
- Phase 12 row: update to show correct plan count once plans exist (will be "0/2" at this point)

Do NOT change Phase 12 plan checkboxes -- those are not yet complete.
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && grep -c '\[ \]' .planning/ROADMAP.md</automated>
  </verify>
  <done>Only Phase 12 items (phase header + plan checkboxes) remain unchecked. All completed plan/phase checkboxes are checked. Progress table rows have consistent column formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PROJECT.md decisions and REQUIREMENTS.md traceability</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
**PROJECT.md:**
1. Read PROJECT.md Key Decisions table
2. Change the "Bed remapping (1-5 -> A-E)" row outcome from "Revisit (v1.1 Phase 7)" to "Resolved -- Phase 7 applied canonical A-B naming across all docs and data"
3. Review any other stale markers in the v1.1 checklist section -- check off items for completed phases (7, 8, 9, 10, 11)

**REQUIREMENTS.md:**
1. Read REQUIREMENTS.md
2. Change DOCS-04 checkbox (line ~89) from `[ ]` to `[x]` -- neighbor-vacation-guide.md exists
3. In v1.0 traceability table: change DOCS-04 status from "Pending" to "Complete"
4. In v1.0 traceability table: change COOK-02 status from "Pending" to "Planned (docs ready, planting Oct 2026)"
5. Do NOT change COOK-02 checkbox -- garlic is not yet physically planted

Preserve all existing content. Only change the specific fields identified above.
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && echo "--- PROJECT.md ---" && grep -c 'Revisit' .planning/PROJECT.md && echo "--- REQUIREMENTS.md ---" && grep 'DOCS-04\|COOK-02' .planning/REQUIREMENTS.md | grep -c 'Pending'</automated>
  </verify>
  <done>PROJECT.md has zero "Revisit" markers for completed work. REQUIREMENTS.md shows DOCS-04 as Complete, COOK-02 as Planned with documentation note. No other Pending statuses exist except PMNT-01 through PMNT-05.</done>
</task>

</tasks>

<verification>
- `grep -c '\[ \]' .planning/ROADMAP.md` returns a small number (only Phase 12 items)
- `grep 'Revisit' .planning/PROJECT.md` returns 0 lines
- `grep 'Pending' .planning/REQUIREMENTS.md` returns only PMNT-01 through PMNT-05 rows
</verification>

<success_criteria>
All three planning markdown files accurately reflect the actual completion state of the project. No stale checkboxes for completed work, no outdated decision outcomes, no incorrect traceability statuses.
</success_criteria>

<output>
After completion, create `.planning/phases/12-planning-artifact-maintenance/12-01-SUMMARY.md`
</output>
